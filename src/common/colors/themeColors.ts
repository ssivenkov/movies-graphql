import { COLORS } from './colors';

const {
  WHITE,
  BLACK,
  TUNDORA,
  SILVER1,
  SHARK,
  SILVER_CHALICE,
  ALTO,
  GALLERY,
  MAKO,
  WOODSMOKE,
} = COLORS;

export const lightThemeColors = [BLACK, WHITE, SILVER1, SILVER_CHALICE, ALTO, GALLERY];

export const darkThemeColors = [WHITE, TUNDORA, SHARK, WOODSMOKE, SHARK, MAKO];
